/**
 * Function component for return header UI.
 *
 * @param   {Object} props
 *
 * @returns {JSX.Element}
 */
function functionalComponent(props) {
    return (
        <h1>Welcome {props.name} to the React Component Tutorial</h1>
    );
}

export default functionalComponent;